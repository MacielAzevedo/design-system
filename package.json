{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "storybook-dark-mode": "^3.0.0",
    "turbo": "^1.10.7"
  }
}
